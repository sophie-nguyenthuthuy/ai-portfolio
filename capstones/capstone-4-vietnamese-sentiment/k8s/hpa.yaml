apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: nlpvi-api
  labels:
    app: nlpvi
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: nlpvi-api
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
