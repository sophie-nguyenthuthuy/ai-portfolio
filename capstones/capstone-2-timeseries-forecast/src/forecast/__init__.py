"""Time-series forecasting capstone (Vietnam retail-sales / stock close).

Light import surface: heavy/optional libs (statsmodels, prophet, torch, mlflow)
are imported lazily inside functions so ``import forecast`` works with base deps.
"""

__version__ = "0.1.0"
