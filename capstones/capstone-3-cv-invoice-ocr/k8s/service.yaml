apiVersion: v1
kind: Service
metadata:
  name: cv-invoice-ocr
  labels:
    app: cv-invoice-ocr
spec:
  type: ClusterIP
  selector:
    app: cv-invoice-ocr
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
