apiVersion: v1
kind: Service
metadata:
  name: research-agent-api
  labels:
    app: research-agent-api
spec:
  type: ClusterIP
  selector:
    app: research-agent-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
